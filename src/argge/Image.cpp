//#include "Component.h"
//#include "Entity.h"
#include <rend/rend.h>
#include <stb/stb_image.h>
#include "Image.h"

namespace argge
{
	void Image::getImage()
	{
		int w = 0;
		int h = 0;
		int channels = 4;
		//data = stbi_load("../src/demo/curuthers/Whiskers_diffuse.png", &w, &h, NULL, channels);
	}
		
	//rend::vec4 Image::getPixel(unsigned int x, unsigned int y)
	//{
	//	//return data.at(size.x * y + x);
	//}
}